server:
  port: 8080

  servlet:
    context-path: /api/v1

spring:
  application:
    name: edusysproapi

  profiles:
    active: [mysql, flyway, GMail, RabbitMQ]

  #REST config
  data:
    rest:
      base-path: /api/v1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Development environment configuration
---
spring:
  config:
    activate:
      on-profile: postgres
  datasource:
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://localhost:5432/academic'
    username: 'postgres'
    password: 'password'

# Development environment configuration
---
spring:
  config:
    activate:
      on-profile: mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: 'jdbc:mysql://localhost:3306/academic'
    username: 'root'
    password: 'password'

# Development environment configuration
---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://34.168.154.185/academic'
    username: 'postgres'
    password: 'Password112022'

# Production environment configuration
---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    driver-class-name: ${DRIVER}
    url: 'jdbc:${DBMS}://${SERVER}:${PORT}/${DATABASE_NAME}'
    username: '${USERNAME}'
    password: '${PASSWORD}'

# Flyway Settings for mysql
---
spring:
  config:
    activate:
      on-profile: flyway
  flyway:
    baseline-on-migrate: true
    validate-on-migrate: true

#JWT Settings
---
jwt:
  secret: mySecretKey12345678901234567890123456789012
  expiration: 86400000
  refresh:
    expiration: 604800000

#GMAIL Setting
---
spring:
  config:
    activate:
      on-profile: GMail
  mail:
    host: smtp.gmail.com
    port: 587
    username: tt9999.tt123@gmail.com
    password: 'vzcj rtse mskr czyj'
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 10000
          timeout: 15000
          writetimeout: 15000
        debug: true

#Prod MAIL Setting
---
spring:
  config:
    activate:
      on-profile: prodMail
  mail:
    host: ${HOST}
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      email:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 10000
          timeout: 15000
          writetimeout: 15000

#RabbitMQ
---
spring:
  config:
    activate:
      on-profile: RabbitMQ
  rabbitmq:
    host: seal.lmq.cloudamqp.com
    port: 5672
    username: ceuftess
    password: 1gLnGKtzJ8LmF3UkChkOhD3WthLE0QN2
    virtual-host: ceuftess

app:
  mail:
    from: "Edusyspro"
    queue:
      exchange: mail.exchange
      queue: mail.queue
      routing-key: mail.send

ui:
  url: http://localhost:3000;http://localhost:5173
  email-base-url: http://localhost:5173